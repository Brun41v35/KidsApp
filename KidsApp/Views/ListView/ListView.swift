import SwiftUI

struct ListView: View {

    var body: some View {

        ZStack {
            Color.yellow.opacity(0.3)
                .ignoresSafeArea()
        }
    }
}

#Preview {
    ListView()
}
